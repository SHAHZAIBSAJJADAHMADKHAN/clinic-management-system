# Cross-System Tests

This directory is reserved for acceptance and integration coverage that spans backend, frontend, and automation boundaries. Focused backend tests live in `backend/tests/`; frontend tests live beside frontend source files.
